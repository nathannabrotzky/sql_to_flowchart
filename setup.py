from setuptools import setup, find_packages

setup(
    name='sql_flowchart',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'sqlparse',
        # Add other dependencies here
    ],
)